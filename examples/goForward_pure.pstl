goForward p = case p of
    (let xs = proj0 p ;
    case xs of
        (ret p)
        (ret p)
        (let bs = proj1 p ;
        let x = proj0 xs ; let xsbis = proj1 xs ;
        let bsbis = ctor1 x bs ;
        let r = ctor0 xsbis bsbis ; ret r))
     (ret p)
     (ret p)
